name: Performance CWV V1

on:
  push:
    branches: [ops/performance-cwv-v1]

jobs:
  pdp-runtime-loader-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CPANEL_HOST: cip20.mizbanfadns.net
      CPANEL_USER: gramissi
      CPANEL_TOKEN: ${{ secrets.CPANEL_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Compile PDP runtime loader scan
        run: python3 -m py_compile .ops/performance-pdp-runtime-loader-scan-v1.py
      - name: Inspect live PDP runtime loaders read only
        run: python3 .ops/performance-pdp-runtime-loader-scan-v1.py
