name: Product Legacy Remediation V1

on:
  push:
    branches: [ops/product-legacy-remediation-v1]

jobs:
  remediation-hold-safe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hold mutation workflows
        run: echo "Product legacy remediation publisher disabled; checkpoint update in progress."
