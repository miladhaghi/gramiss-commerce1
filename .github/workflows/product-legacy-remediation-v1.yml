name: Product Legacy Remediation V1

on:
  push:
    branches: [ops/product-legacy-remediation-v1]

jobs:
  remediation-checkpoint-prep-readonly:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Confirm pants publisher is inactive
        run: |
          python3 -m py_compile .ops/legacy-pants-batch6-publish.py
          echo "Pants mutation publisher disabled; checkpoint preparation only."
