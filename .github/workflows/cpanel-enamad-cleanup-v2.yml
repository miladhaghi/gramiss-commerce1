name: cPanel eNAMAD Final Cleanup V2

on:
  pull_request:
    branches: [main]

jobs:
  cleanup:
    if: github.head_ref == 'ops/enamad-final-cleanup-v2'
    runs-on: ubuntu-latest
    env:
      CPANEL_HOST: cip20.mizbanfadns.net
      CPANEL_USER: gramissi
      CPANEL_TOKEN: ${{ secrets.CPANEL_TOKEN }}
      THEME_ROOT: public_html/wp-content/themes/gramiss-theme-next
    steps:
      - uses: actions/checkout@v4
        with:
          ref: wordpress-migration
      - name: Final cleanup and LiteSpeed verification
        run: python3 deploy/enamad/purge_litespeed_enamad_v2.py
