name: cPanel Shop Related Final Polish

on:
  pull_request:
    branches: [main]

jobs:
  deploy:
    if: github.head_ref == 'ops/shop-related-final-polish-disabled'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy operational deploy disabled; live state is newer."
